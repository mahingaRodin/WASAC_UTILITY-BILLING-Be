$kind: http-request
name: Get Readings by Meter
method: GET
url: '{{baseUrl}}/api/meter-readings/meter/{{meterId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
