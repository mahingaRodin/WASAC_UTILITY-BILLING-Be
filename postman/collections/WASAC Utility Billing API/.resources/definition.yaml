$kind: collection
name: WASAC Utility Billing API
description: 'Complete API collection for the WASAC Utility Billing system. Base URL: {{baseUrl}}. Use the Auth folder to get a JWT token and set it as {{authToken}}.'
variables:
  - key: baseUrl
    value: 'http://localhost:4000'
