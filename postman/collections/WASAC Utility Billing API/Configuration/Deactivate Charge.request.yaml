$kind: http-request
name: Deactivate Charge
method: PATCH
url: '{{baseUrl}}/api/config/charges/{{chargeId}}/deactivate'
order: 7000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
