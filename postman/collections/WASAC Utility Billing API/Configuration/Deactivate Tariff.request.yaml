$kind: http-request
name: Deactivate Tariff
method: PATCH
url: '{{baseUrl}}/api/config/tariffs/{{tariffId}}/deactivate'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
