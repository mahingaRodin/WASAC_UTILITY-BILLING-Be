$kind: http-request
name: Reject Payment
method: POST
url: '{{baseUrl}}/api/payments/{{paymentId}}/reject'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
